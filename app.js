import * as THREE from "https://cdn.jsdelivr.net/npm/three@0.165.0/build/three.module.js";import {OrbitControls} from "https://cdn.jsdelivr.net/npm/three@0.165.0/examples/jsm/controls/OrbitControls.js";
const canvas=document.querySelector('#space'),scene=new THREE.Scene();scene.fog=new THREE.FogExp2(0x05050a,.018);const camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,1000);camera.position.set(0,1.5,18);const renderer=new THREE.WebGLRenderer({canvas,antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.outputColorSpace=THREE.SRGBColorSpace;const controls=new OrbitControls(camera,canvas);controls.enableDamping=true;controls.enablePan=false;controls.minDistance=8;controls.maxDistance=25;controls.autoRotate=true;controls.autoRotateSpeed=.22;controls.enableZoom=false;
const core=new THREE.Mesh(new THREE.IcosahedronGeometry(3.2,5),new THREE.MeshStandardMaterial({color:0x7468ff,emissive:0x372f9c,emissiveIntensity:1.7,roughness:.25,metalness:.15,wireframe:false}));scene.add(core);const shell=new THREE.Mesh(new THREE.IcosahedronGeometry(4.25,2),new THREE.MeshBasicMaterial({color:0xa59cff,wireframe:true,transparent:true,opacity:.075}));scene.add(shell);const ring=new THREE.Mesh(new THREE.TorusGeometry(5.4,.018,16,180),new THREE.MeshBasicMaterial({color:0x9c91ff,transparent:true,opacity:.45}));ring.rotation.x=Math.PI/2.8;scene.add(ring);const ring2=ring.clone();ring2.scale.set(.72,.72,.72);ring2.rotation.x=1.1;ring2.rotation.y=.7;scene.add(ring2);
const starsGeo=new THREE.BufferGeometry(),count=1800,pos=new Float32Array(count*3);for(let i=0;i<count;i++){const r=THREE.MathUtils.randFloat(9,90),a=Math.random()*Math.PI*2,b=Math.acos(THREE.MathUtils.randFloatSpread(2));pos[i*3]=r*Math.sin(b)*Math.cos(a);pos[i*3+1]=r*Math.cos(b);pos[i*3+2]=r*Math.sin(b)*Math.sin(a)}starsGeo.setAttribute('position',new THREE.BufferAttribute(pos,3));scene.add(new THREE.Points(starsGeo,new THREE.PointsMaterial({color:0xffffff,size:.035,transparent:true,opacity:.7})));
scene.add(new THREE.AmbientLight(0x5e56aa,1.5));const light=new THREE.PointLight(0x8f7cff,35,30);light.position.set(4,4,8);scene.add(light);const light2=new THREE.PointLight(0x53d8ff,18,25);light2.position.set(-7,-2,4);scene.add(light2);
let tx=0,ty=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*.7;ty=(e.clientY/innerHeight-.5)*.4});function animate(t){requestAnimationFrame(animate);core.rotation.x=t*.00012;core.rotation.y=t*.00018;shell.rotation.y=-t*.00008;ring.rotation.z=t*.00015;camera.position.x+=(tx-camera.position.x*.02)*.025;camera.position.y+=(1.5-ty-camera.position.y*.02)*.025;controls.update();renderer.render(scene,camera)}animate(0);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});
const modal=document.querySelector('#modal');const open=()=>modal.classList.add('open');document.querySelector('#launch').onclick=open;document.querySelector('#enter').onclick=open;document.querySelector('#close').onclick=()=>modal.classList.remove('open');document.querySelector('#close2').onclick=()=>modal.classList.remove('open');document.querySelector('#reveal').onclick=()=>document.querySelector('#about').scrollIntoView({behavior:'smooth'});addEventListener('keydown',e=>{if(e.key==='Escape')modal.classList.remove('open')});