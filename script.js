const c=document.getElementById('space'),x=c.getContext('2d');let w,h,dots=[];function resize(){w=c.width=innerWidth*devicePixelRatio;h=c.height=innerHeight*devicePixelRatio;x.scale(devicePixelRatio,devicePixelRatio);w=innerWidth;h=innerHeight;dots=Array.from({length:180},()=>({x:Math.random()*w,y:Math.random()*h,z:Math.random()*2+.2,s:Math.random()*1.7+.2}))}resize();addEventListener('resize',resize);let mx=0,my=0;addEventListener('mousemove',e=>{mx=(e.clientX/w-.5)*18;my=(e.clientY/h-.5)*18});function draw(){x.clearRect(0,0,w,h);for(const p of dots){p.y-=p.s;if(p.y<0)p.y=h;let px=p.x+mx*p.s,py=p.y+my*p.s;x.globalAlpha=.25+p.s*.25;x.fillStyle='#fff';x.beginPath();x.arc(px,py,p.s,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}draw();const m=document.getElementById('modal');document.getElementById('enter').onclick=()=>m.classList.add('open');document.getElementById('close').onclick=()=>m.classList.remove('open');